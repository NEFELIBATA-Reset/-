class Solution {
    public int lengthOfLastWord(String s) {
        String[] arr=s.split(" ");
        if(arr.length==0){
            return 0;
        }
        char[] a=arr[arr.length-1].toCharArray();
        return a.length;
    }
}
